package auton.routines;

import auton.commands.*;

public class Defense extends SequentialCommand {
	public Defense() {
		// super(new RunIntake(1.0),
		// new Wait(1000),
		// new RunIntake(-1.0),
		// new Wait(2000),
		// new RunIntake(1.0),
		// new RunIntake(0.0));
	}
}